str = 'adesina afeez'

// const toUpperCase = str => str.toUpperCase()

const name = function toUpperCase(str){
        return str.toUpperCase()
    }

console.log(name(str))

toLowerCase = str => str.toLowerCase();
